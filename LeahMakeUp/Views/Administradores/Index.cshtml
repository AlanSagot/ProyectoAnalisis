@model IEnumerable<DAL.Administrador>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreAdministrador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PasswordAdmin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sucursal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreAdministrador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PasswordAdmin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sucursal.Direccion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdministradorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdministradorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdministradorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
