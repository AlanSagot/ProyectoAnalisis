<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Leah Make Up</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="Free HTML Templates">
    <meta name="description" content="Free HTML Templates">

    <!-- Favicon -->
    <link rel="icon" href="~/img/favicon.ico">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="~/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="~/css/style.css" rel="stylesheet">
    <script src="~/js/index.js"></script>

    <link href="~/css/chat.css" rel="stylesheet">
    <link href="~/img" rel="stylesheet">
</head>

<body>
    <!-- Topbar Start -->
    <div class="container-fluid">
        <div class="row bg-secondary py-2">
            <div class="col-lg-6 d-none d-lg-block">
                <div class="d-inline-flex align-items-center">
                    <a class="text-dark" href="">Preguntas Frecuentes</a>
                </div>
            </div>
            <div class="col-lg-6 text-center text-lg-right">
                <div class="d-inline-flex align-items-center">
                    <a class="text-dark px-2" href="https://wa.me/71397781?text=Buenas%2C%20estoy%20interesada%20en%20un%20producto%21">
                        <i class="fab fa-whatsapp"></i>
                    </a>
                    <a class="text-dark px-2" href="https://www.instagram.com/leah_makeup_and_accesories/">
                        <i class="fab fa-instagram"></i>
                    </a>
                </div>
            </div>
        </div>
        <div class="row align-items-center py-3">
            <div class="col-lg-3 col-md-6 col-sm-12">
                <a href="" class="text-decoration-none">
                    <h1 class="m-0 display-5 font-weight-semi-bold"><span class="text-primary font-weight-bold border px-3 mr-1">L</span>Leah Make Up</h1>
                </a>
            </div>
            <!-- BUSCAR -->
            <div class="col-lg-6 col-md-6 col-sm-12 text-left">
                <form id="searchForm">
                    <div class="input-group">
                        <input type="text" id="searchInput" class="form-control" placeholder="Buscar productos">
                        <div class="input-group-append">
                            <span class="input-group-text bg-transparent text-primary">
                                <i class="fa fa-search"></i>
                            </span>
                        </div>
                    </div>
                </form>
            </div>
            <div class="col-lg-3 col-md-6 col-sm-12 text-right">
                <a asp-controller="Home" asp-action="Index" class="btn border">
                    <i class="fas fa-home text-primary"></i>
                    <span class="badge">0</span>
                </a>
            </div>
        </div>
    </div>
    <!-- Topbar End -->
    <!-- Main Content Start -->
    <div class="container">
        @model IEnumerable<DAL.Carrito>

        @{
            ViewData["Title"] = "Index";
            Layout = "~/Views/Shared/_Layout.cshtml";
        }

        <table class="table">
            <thead>
                <tr>
                    <th></th>
                    <th>
                        @Html.DisplayNameFor(model => model.Inventario.NombreProducto)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Cantidad)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Subtotal)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.PrecioTotal)
                    </th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @if (item.Inventario.FotoProducto != null)
                            {
                                var imageBase64 = Convert.ToBase64String(item.Inventario.FotoProducto);
                                var imageSrc = string.Format("data:image/jpeg;base64,{0}", imageBase64);
                                <img src="@imageSrc" alt="Producto" width="50" height="50" />
                            }
                        </td>
                        <td>
                            <span class="precio-unitario" data-precio="@item.Inventario.NombreProducto">@item.Inventario.NombreProducto</span>
                        </td>
                        <td>
                            <div class="counter">
                                <button type="button" class="decrement">-</button>
                                <span class="count" id="count-@item.CarritoId">@item.Cantidad</span>
                                <button type="button" class="increment">+</button>
                            </div>
                            <input type="hidden" class="cantidad-input" value="@item.Cantidad" data-id="@item.CarritoId" />
                        </td>
                        <td>
                            <span class="precio-unitario" data-precio="@item.Inventario.PrecioXVenta">@item.Inventario.PrecioXVenta</span>
                        </td>
                        <td>
                            <span class="precio-total" id="precio-total-@item.CarritoId">@item.PrecioTotal</span>
                        </td>
                        <td>
                            <form asp-action="RemoveFromCart" method="post" class="d-inline">
                                <input type="hidden" name="id" value="@item.CarritoId" />
                                <button type="submit" class="btn btn-danger"><i class="fa fa-trash"></i></button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <!-- Precio Final -->
        <div class="text-right">
            <h4>Precio Final: <span id="precio-final">0.00</span></h4>
        </div>

        <!-- Botón para eliminar todos los productos del carrito -->
        <form asp-action="ClearCart" method="post">
            <button type="submit" class="btn btn-danger">Eliminar todos los productos</button>
        </form>

    </div>
    <!-- Main Content End -->

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const counters = document.querySelectorAll('.counter');
            const precioFinalElem = document.getElementById('precio-final');

            const updateFinalPrice = () => {
                let totalPrice = 0;
                document.querySelectorAll('.precio-total').forEach(elem => {
                    totalPrice += parseFloat(elem.textContent) || 0;
                });
                precioFinalElem.textContent = totalPrice.toFixed(2);
            };

            counters.forEach(counter => {
                const decrementBtn = counter.querySelector('.decrement');
                const incrementBtn = counter.querySelector('.increment');
                const countDisplay = counter.querySelector('.count');
                const cantidadInput = counter.nextElementSibling;
                const precioUnitarioElem = counter.parentElement.nextElementSibling.querySelector('.precio-unitario');
                const precioTotalElem = counter.parentElement.nextElementSibling.nextElementSibling.querySelector('.precio-total');

                let count = parseInt(countDisplay.textContent);
                const precioUnitario = parseFloat(precioUnitarioElem.dataset.precio);
                const carritoId = cantidadInput.dataset.id;

                const updateTotal = () => {
                    const newTotal = count * precioUnitario;
                    precioTotalElem.textContent = newTotal.toFixed(2);
                    updateFinalPrice(); // Actualizar el precio final
                };

                const updateQuantityOnServer = (newCount) => {
                    $.ajax({
                        url: '@Url.Action("UpdateQuantity", "Carritos")',
                        type: 'POST',
                        data: {
                            id: carritoId,
                            nuevaCantidad: newCount
                        },
                        success: function (response) {
                            console.log('Cantidad actualizada en el servidor');
                        },
                        error: function (error) {
                            console.error('Error al actualizar la cantidad en el servidor');
                        }
                    });
                };

                decrementBtn.addEventListener('click', () => {
                    if (count > 1) {
                        count--;
                        countDisplay.textContent = count;
                        cantidadInput.value = count;
                        updateTotal();
                        updateQuantityOnServer(count);
                    }
                });

                incrementBtn.addEventListener('click', () => {
                    count++;
                    countDisplay.textContent = count;
                    cantidadInput.value = count;
                    updateTotal();
                    updateQuantityOnServer(count);
                });
            });

            // Inicializar el precio final al cargar la página
            updateFinalPrice();
        });
    </script>

    <!-- Footer Start -->
    <div class="container-fluid bg-dark text-light mt-5 pt-5 px-0">
        <div class="container py-5">
            <div class="row pt-5">
                <div class="col-lg-4 col-md-6 mb-5 mb-md-0">
                    <h5 class="text-light mb-4">Cita</h5>
                    <p class="mb-4">“Tu belleza es mi pasión y mi arte. Estoy aquí para realzar tu belleza natural y hacer que te sientas segura y radiante.”</p>
                </div>
                <div class="col-lg-4 col-md-6 mb-5 mb-md-0">
                    <h5 class="text-light mb-4">Enlaces Rápidos</h5>
                    <div class="d-flex flex-column">
                        <a class="text-light mb-2" href="#">Inicio</a>
                        <a class="text-light mb-2" href="#">Productos</a>
                        <a class="text-light mb-2" href="#">Carrito</a>
                        <a class="text-light mb-2" href="#">Contacto</a>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6 mb-5 mb-md-0">
                    <h5 class="text-light mb-4">Ubicación</h5>
                    <p class="mb-4">Leah Make Up</p>
                    <p class="mb-4">Dirección: Tu Dirección</p>
                    <p class="mb-4">Teléfono: Tu Teléfono</p>
                </div>
            </div>
        </div>
    </div>
    <!-- Footer End -->

</body>

</html>
